using System;

public class Operations
{
    public enum Operation
    {
        Add = '+',
        Subtract = '-',
        Multipy = '*',
        Divide = '/',
        Pow = '^',
        ErrorOperation
    }
}
